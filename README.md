# ML-project-Zomato

1. Create new Environment 
conda create -p zenv python=3.9 -y

2. Activate Environment
conda activate zenv   --> using CMD

source activate zenv --> using Gitbash

3. Install requirements file
pip install -r requrements.txt